# gcf/eval/requirements.txt — dependencies for the eval Cloud Function.
#
# GCF python311 source-deploy requires a requirements.txt in the function
# directory.  List only what main.py actually imports; versions are kept in
# sync with the root requirements.txt.

# Cloud Function HTTP runtime (provides flask internally)
functions-framework>=3.0,<4.0

# Storage
google-cloud-firestore>=2.16

# Config / serialisation
pydantic>=2.0,<3.0
pyyaml>=6.0
python-dotenv>=1.0
